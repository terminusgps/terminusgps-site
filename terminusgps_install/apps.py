from django.apps import AppConfig


class TerminusgpsInstallConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "terminusgps_install"
    verbose_name = "Terminus GPS Install"
